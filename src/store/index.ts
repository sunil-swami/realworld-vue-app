import Vue from 'vue';
import Vuex from 'vuex';
import users from './modules/users';

Vue.use(Vuex);

const state = {
};
const mutations = {
};
const actions = {
};
const modules = {
  users ,
};
export default new Vuex.Store({
  state,
  actions,
  mutations,
  modules,
});
